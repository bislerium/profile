---
import { STACK } from 'src/pages/portfolio/_constants';

function slug(name: string): string {
  return `stack-${name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')}`;
}
---
<section class="tech-stack" aria-labelledby="stack-heading">
  <h2 id="stack-heading" class="label tech-label">Stack</h2>
  <ul class="tech-list" role="list">
    {STACK.map(item => (
      <li class="stack-parent">
        <button
          type="button"
          class="stack-category"
          aria-expanded="false"
          aria-controls={slug(item.name)}
        >
          {item.name}
        </button>
        <ul id={slug(item.name)} class="stack-sublist" role="list">
          {item.items.map(sub => (
            <li class="stack-subitem">{sub}</li>
          ))}
        </ul>
      </li>
    ))}
  </ul>
</section>
