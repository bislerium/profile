# Cloudflare Worker — Markdown for Agents
# Deployed via GitHub integration on push to main
#
# Route must be set manually in dashboard:
# bishalgc.info.np → Workers Routes → Add route → bishalgc.info.np/* → markdown-for-agents

name = "markdown-for-agents"
main = "workers/markdown-for-agents.js"
compatibility_date = "2026-08-01"

[observability.logs]
enabled = true
invocation_logs = true
